import React from 'react';
import TitleBar from './TitleBar';

export default class extends React.Component {
    render() {
        return (
            < >
                <TitleBar />
            </>
        );
    }
};